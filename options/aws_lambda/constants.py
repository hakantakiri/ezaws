# Constants for lambda options

OPTION_NAME = "lambda"
CLI_HELP = "aws lambda service"
CLI_CHOICES = [
        "list-functions",
       "get-function",
	"update-function"
       ]
CLI_ALIASES = [
        "la"
        ]

